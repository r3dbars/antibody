name: publish
# Publishes to npm via trusted publishing (OIDC) on version tags or a
# manual dispatch. Ordinary pushes to main never publish.
# npm-side config: npmjs.com → antibody → Settings → Trusted publisher →
# GitHub Actions → r3dbars/antibody, publish.yml
on:
  push:
    tags: ['v*']
  workflow_dispatch: {}
permissions:
  id-token: write   # OIDC token for npm trusted publishing
  contents: read
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
      - run: npm install -g npm@latest   # OIDC publish needs npm >= 11.5
      - run: npm ci
      - run: npm test
      - run: npm run typecheck
      - name: skip if version already published
        id: check
        run: |
          CURRENT=$(node -p "require('./package.json').version")
          TAG="${GITHUB_REF_NAME:-}"
          if [[ "$TAG" == v* && "$TAG" != "v$CURRENT" ]]; then
            echo "tag $TAG does not match package.json version $CURRENT" >&2
            exit 1
          fi
          PUBLISHED=$(npm view antibody version 2>/dev/null || echo none)
          echo "published=$PUBLISHED current=$CURRENT tag=$TAG"
          if [ "$PUBLISHED" = "$CURRENT" ]; then echo "skip=true" >> "$GITHUB_OUTPUT"; fi
      - name: publish
        if: steps.check.outputs.skip != 'true'
        run: npm publish
